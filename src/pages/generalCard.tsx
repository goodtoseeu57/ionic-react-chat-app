import { IonCard, IonCardContent } from "@ionic/react";

const Card: React.FC<any> = (props) => {
    return ( <IonCard>
        <IonCardContent>
            my content
        </IonCardContent>
    </IonCard> );
}

export default Card;